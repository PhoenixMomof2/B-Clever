import React from 'react'

const ParentDashboard = () => {
  return (
    // create a pie chart so the parent can see the savings, wants, needs of kid
    // create a form that allows for giving allowances
    // add web sockets so parent can chat with other parents and kids about their budgeting
    <div className="flex items-center justify-center h-screen">    
      <div className="bg-indigo-800 text-white font-bold rounded-lg border shadow-lg p-10">
        PARENT DASHBOARD
      </div>	
    </div>
  )
}
export default ParentDashboard